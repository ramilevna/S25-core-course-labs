apiVersion: v1
kind: Service
metadata:
  name: app-python
  namespace: default
spec:
  type: NodePort
  selector:
    app: app-python
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
#---
#apiVersion: v1
#kind: Service
#metadata:
#  name: app-javascript
#spec:
#  selector:
#    app: app-javascript
#  ports:
#    - protocol: TCP
#      port: 6000
#      targetPort: 6000
#  type: NodePort