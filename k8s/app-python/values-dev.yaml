replicaCount: 1

image:
  repository: ramilevna/app_python
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 5000

env:
  - name: ENV

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"

autoscaling:
  enabled: false